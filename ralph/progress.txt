# Ralph Progress Log
Started: 2026-04-15 22:54 +08:00
---

## Codebase Patterns
- `ConversationWorkflow` remains the contract hub for chat requests, plan persistence, and structured result shaping; shared response changes should still flow through `ConversationResultSupport` and `web/src/types/api.ts`.
- Productization features now span a stable cross-layer path: backend DTO/domain contract -> SQLite persistence -> `ConversationDetailResponse` -> Pinia store -> workspace panels.

## 2026-04-15 23:40 +08:00 - US-001 through US-007
- Completed the real trip-constraint pass by persisting `origin`, `startDate`, `endDate`, and `travelers`, and by accepting structured brief patches directly in chat requests.
- Added the trip-brief editor to the workspace and kept free-form chat requests backward compatible.
- Derived real itinerary dates from `startDate` and surfaced estimate-mode messaging when dates are still not locked.
- Kept trust metadata consistent on hotels and transit legs, and preserved the UI grounding / fallback visibility already in the plan surfaces.
- Key files changed:
  - `travel-agent-app/src/main/java/com/travalagent/app/dto/ChatRequest.java`
  - `travel-agent-app/src/main/java/com/travalagent/app/dto/TripBriefRequest.java`
  - `travel-agent-app/src/main/java/com/travalagent/app/service/ConversationWorkflow.java`
  - `travel-agent-domain/src/main/java/com/travalagent/domain/model/entity/TaskMemory.java`
  - `travel-agent-domain/src/main/java/com/travalagent/domain/model/entity/TravelPlanDay.java`
  - `travel-agent-domain/src/main/java/com/travalagent/domain/model/entity/TravelHotelRecommendation.java`
  - `travel-agent-domain/src/main/java/com/travalagent/domain/model/entity/TravelTransitLeg.java`
  - `travel-agent-infrastructure/src/main/java/com/travalagent/infrastructure/repository/SqliteConversationRepository.java`
  - `web/src/components/ChatPanel.vue`
  - `web/src/components/PlanPanel.vue`
  - `web/src/stores/chat.ts`
  - `web/src/types/api.ts`
---

## 2026-04-15 23:40 +08:00 - US-008 through US-016
- Added scoped replan support for `HOTEL_AREA` and `DAY`, including request contract, workflow merge logic, and workspace controls that refresh only the affected slice.
- Added persistent pre-departure checklist items, a checklist update endpoint, and an execution panel that lets the user confirm or reopen checklist items.
- Added plan version snapshot persistence and recent-version diff summaries exposed through conversation detail and rendered in the workspace.
- Key files changed:
  - `travel-agent-app/src/main/java/com/travalagent/app/dto/ReplanScopeRequest.java`
  - `travel-agent-app/src/main/java/com/travalagent/app/dto/ConversationChecklistUpdateRequest.java`
  - `travel-agent-app/src/main/java/com/travalagent/app/dto/TravelPlanVersionDiffResponse.java`
  - `travel-agent-app/src/main/java/com/travalagent/app/controller/ConversationController.java`
  - `travel-agent-app/src/main/java/com/travalagent/app/service/ConversationApplicationService.java`
  - `travel-agent-domain/src/main/java/com/travalagent/domain/model/entity/TravelChecklistItem.java`
  - `travel-agent-domain/src/main/java/com/travalagent/domain/model/entity/TravelPlan.java`
  - `travel-agent-domain/src/main/java/com/travalagent/domain/model/entity/TravelPlanVersionSnapshot.java`
  - `travel-agent-domain/src/main/java/com/travalagent/domain/repository/ConversationRepository.java`
  - `travel-agent-app/src/main/resources/schema.sql`
  - `web/src/components/PlanExecutionPanel.vue`
  - `web/src/App.vue`
---

## 2026-04-15 23:40 +08:00 - US-017 through US-019
- Added internal-beta quality scenario fixtures plus a replay runner that writes JSON and Markdown reports for product review.
- Extended feedback summary / export filters to support destination, agent type, feedback scope, reason label, and plan-version time-window filtering.
- Mounted a filtered feedback operations panel in the workspace so product and ops can inspect recent failures without raw logs.
- Key files changed:
  - `scripts/quality-scenarios.json`
  - `scripts/run_quality_scenarios.py`
  - `web/src/components/FeedbackLoopPanel.vue`
  - `web/src/stores/chat.ts`
---

## Verification
- Passed: `.\mvnw.cmd -B test`
- Passed: `cd web && npm.cmd run build`
- Passed: `cd web && npm.cmd run test`
- Passed: `python .\scripts\run_quality_scenarios.py --help`
- Not run: full quality-scenario replay, because it requires a running app instance with usable planner/model configuration.
- Not run: browser-only visual verification via `dev-browser`, because that skill/tooling was not available in this environment.

## Outcome
- The current `ralph/prd.json` (`ralph/mvp-to-beta-productization`) is now aligned with the codebase and no longer points at an unrelated progress log.

---

## 2026-04-16 00:51 +08:00 - PRD status reconciliation
- Re-ran `.\mvnw.cmd -B test`, `cd web && npm.cmd run build`, and `cd web && npm.cmd run test`; all passed on the current worktree.
- Confirmed that the frontend build still performs `vue-tsc --noEmit`, so the rerun build covered the frontend typecheck gate.
- Attempted to run the quality-scenario replay against a locally booted app using the existing `browser-test` / local-tooling path, but the scripted runtime path was not startable as-is in this environment; the replay therefore remains unverified.
- Updated `ralph/prd.json` status flags to reflect actual evidence:
  - Passed: `US-001`, `US-002`, `US-004`, `US-006`, `US-008`, `US-009`, `US-011`, `US-012`, `US-014`, `US-015`, `US-018`
  - Still pending with notes: `US-003`, `US-005`, `US-007`, `US-010`, `US-013`, `US-016`, `US-017`, `US-019`

---

## 2026-04-16 09:48 +08:00 - Local demo startup and quality replay
- Root-cause analysis for startup failures:
  - `spring-boot:run` from the repository root with `-pl travel-agent-app -am` targets the aggregator POM and fails because the root project has no `mainClass`.
  - In PowerShell, unquoted `-Dspring-boot.run.profiles=...` arguments are split before Maven receives them.
- Added a main-source `local-demo` profile so the application can boot without OpenAI, MCP, or Milvus by wiring stub chat/vector dependencies and local tool settings.
- Verified the backend boots with:
  - `.\mvnw.cmd "-Dspring-boot.run.profiles=local-demo" -f travel-agent-app\pom.xml spring-boot:run`
- Verified runtime behavior on the local-demo instance:
  - `GET /actuator/health` returned `UP`
  - `POST /api/conversations/chat` returned `code=0000`, `agentType=TRAVEL_PLANNER`, and a structured `travelPlan`
- Ran `python .\scripts\run_quality_scenarios.py --base-url http://localhost:8080 --output-dir .\data\exports\quality-reports`, which successfully wrote JSON and Markdown reports.
- Updated `README.md` and `README.zh-CN.md` to document:
  - the correct module-scoped Spring Boot launch commands
  - the PowerShell quoting requirement for `-D...`
  - the new local-demo startup path
  - the quality-scenario replay command
- Updated `ralph/prd.json` so `US-017` is now marked passed.

---

## 2026-04-17 02:20 +08:00 - Browser acceptance closure for remaining UI stories
- Fixed three issues that blocked browser-level closure in `local-demo`:
  - Preserved structured brief fields during conversation finalization by re-extracting memory from `workingMemory` instead of the stale stored snapshot.
  - Preserved and heuristically recognized `startDate`, `endDate`, and `travelers` in the fallback task-memory extractor.
  - Allowed heuristic planner routing to use existing task memory for scoped follow-up requests such as hotel/day replans.
  - Removed a `Map.of(...)` null-value crash in user-message metadata for partial brief updates.
- Added regression coverage for:
  - structured brief fields surviving finalization
  - heuristic planner routing for scoped follow-up requests with existing task memory
- Re-ran `.\mvnw.cmd -B test`; all backend/module tests passed after the fixes.
- Performed headless browser acceptance against `local-demo` on `2026-04-17` and verified:
  - `US-003`: trip brief editor renders missing markers and submits structured brief payload
  - `US-005`: itinerary UI renders real dates (`2026-05-02`, `2026-05-03`)
  - `US-007`: trust-state copy renders on stay recommendations
  - `US-010`: hotel-area and day-level replan controls both render and surface refreshed banners
  - `US-013`: checklist interaction updates the pending count immediately
  - `US-016`: recent version comparison renders after scoped replans
  - `US-019`: feedback operations filters work for destination, agent, scope, and reason label
- `ralph/prd.json` now marks all productization stories `US-001` through `US-019` as passed.
